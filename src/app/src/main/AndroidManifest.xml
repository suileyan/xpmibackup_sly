<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 17+ 本地网络保护：targetSdk 37 默认阻止局域网访问，需显式声明（HIGH-23）
         注意：SMB/WebDAV 实际网络请求运行在 com.miui.backup 宿主进程，
         模块声明仅覆盖模块自身（MainActivity 连通性测试等），宿主侧依赖小米备份 APK 自身声明 -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <!-- legacy 存储权限（READ/WRITE_EXTERNAL_STORAGE）已随 minSdk 30 移除：
         Android 11+ 分区存储下由 MANAGE_EXTERNAL_STORAGE 全盘文件访问覆盖 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Root 模块备份：识别当前管理器（SukiSU/KernelSU/APatch/Magisk）以动态命名条目 -->
    <queries>
        <package android:name="com.sukisu.ultra" />
        <package android:name="com.sukisu.kpm" />
        <package android:name="me.weishu.kernelsu" />
        <package android:name="me.bmax.apatch" />
        <package android:name="io.github.huskydg.magisk" />
        <package android:name="com.topjohnwu.magisk" />
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true">
        <!-- usesCleartextTraffic（HIGH-25）：targetSdk ≥ 28 默认禁止明文 HTTP，
             而「备份至 PC」的局域网通道必然是 http://192.168.x.x:8321（配对 +
             WebDAV），自建 NAS/WebDAV 也多是 http。默认策略下 127.0.0.1 被放行、
             局域网地址被拦 —— 症状正是「USB 能用、局域网永远连不上」，
             且报错只落在日志里（Cleartext HTTP traffic to 192.168.x.x not permitted）。
             云盘等公网接口均为 https，放开明文不影响其安全性。 -->

        <!-- 主页 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- Xposed模块标识 -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />

        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

    </application>

</manifest>
