# 更新日志

本文件记录各版本的主要变更；完整 commit 级明细见 [GitHub Releases](https://github.com/suileyan/xpmibackup_sly/releases)。

## v1.0.0（2026-09-22）· 首个正式版

从 v0.9.5 起累计 15 个提交 + 本轮集中修复，覆盖「备份至 PC」全链路打通、手机端峰值占用、进度可视化与四家云盘的实测缺陷。

### 备份至 PC（mibackpc）

- **USB 通道优先于局域网**：电脑端在「设备在线但 `adb reverse` 未建立」时自动建通道，不再依赖手动点控制页「重连」；手机发现 `127.0.0.1` 有应答即把「电脑备份」方案切到 USB，**仅在电脑名与已配对的一致时切换**（点对点隧道只能连到插着的那台电脑，切错会把备份写到别的机器），拔线后自动回落局域网，全程无需重新配对
- **通道切换对宿主即时生效**：UI 进程改方案后宿主进程仍持旧缓存的坑已修——方案文件的 `mtime + 长度` 变化即让两个进程整体丢弃 Provider 缓存
- **PC 端新增磁盘空间预检**：PUT 前检查可用空间，不足直接返回 507 并给出可读原因，避免把接收端磁盘写满后落一堆半截文件
- **PC 端失败日志不再刷屏**：`adb` 相关探测改为只在「结论变化」时记录（此前手机仅充电时会每轮退避打一条）
- 状态文本区分「无 adb / 无设备 / 未授权 / 通道未建立」，LAN 发现通道失聪、Android 17 本地网络保护、宿主进程私网明文策略等连接问题一并修正

### 手机端存储占用与传输

- **峰值占用大幅下降**：切片前先取「在途分片额度」（背压），磁盘分片锁死在 `(并发+2) × chunk_size`（默认 ≤640MB，逐项模式 ≤192MB）；不再把整个源文件复制一份再传；分片改落模块专属目录 `AllBackupTemp/miback/`
- **孤儿分片自动清理**：进程被强杀留下的分片/临时清单会在下次上传时回收（6 小时阈值，不误伤在途分片）。旧实现实测一次取消残留 97 片 ≈ 6.36GB
- **取消即停**：取消备份后 1~2 秒内停止上传（最多再传完当前一片），不再出现「已在别处取消，这边还继续传十几片」
- **失败/取消不再删本地源文件**：上传返回值参与 `auto_delete_local` 判定，避免「传失败还把本地备份删了」
- 补齐 `Future.cancel` 场景下排队分片的清理（任务自身 `finally` 不会执行）

### 进度与界面

- **进度口径修正**：按宿主自己的分母折算上报，备份项进度不再长时间贴住 30% 再直接跳 100%
- **实时上传速率**：备份页总进度条右侧新增每秒刷新的速率角标
- 配置界面按小米 HyperOS 设计语言重构；深色模式渲染与交互补全；网页登录页完成按钮/标题层级修复
- 备份页新增两个开关：自动删除本地已上传文件、逐项串行备份

### 云盘通道

- **百度网盘**：修复目录创建参数（改为官方规范的 `path` + `isdir=1` + `rtype=0`）并在创建后回查，避免「createFolder 说已存在、listDir 说不存在」导致的整项备份失败；补齐 `-8`（已存在）/`-10`（容量已满）错误码语义
- **光鸭云盘**：修复根目录查询返回无 `data` 的响应被当成「空列表」（表现为备份成功但恢复列表为空、`descript.xml` 报不存在）——空 parentId 退到 `"0"` 重试，仍失败则显式抛错并带响应摘要
- **阿里云盘**：修复恢复列表为空（下载 403 哑失败 + 列表错误体被静默当空）、分片上传 403、坏账号残留与签名自愈
- **联通沃盘**：修复登录页白屏——主文档禁缓存 + 白屏自愈（CDN 缓存了旧 `index.html`，其引用的 JS 已下线，被 Nginx 回退成 HTML 后浏览器当作 JS 解析报 `Unexpected token '<'`）
- 123 云盘自 v0.9.5 起暂停支持；115 网盘自 v0.8.0 起已撤销支持

### 稳定性与安全

- 修复 `Secp256k1` 使用 `BigInteger.TWO`（API 33 才存在）：在 Android 11/12 设备上取该字段抛 `NoSuchFieldError`，属 `Error`，`catch (Exception)` 拦不住，会直接崩进程
- 配置文件统一收纳到 `/MIUI/backup/sly/`，首次启动自动迁移（rename 优先、跨进程幂等）
- 凭据解密失败不再静默；`adb`/TLS 等降级路径全部留痕
- 模块日志分「主日志 + `_err.log`（只记 ERROR 段并附出错前流程）」，敏感字段脱敏

### 升级注意

- 模块自身文件（`config.ini`、`creds.json`、`profiles.json`、`cloud_accounts.json`、`backup_target.json`、`logs/`）会自动迁到 `/sdcard/MIUI/backup/sly/`，无需手动搬
- **装完模块 APK 后需把宿主 `com.miui.backup` 强制停止再拉起**，宿主才会加载新模块代码
- 从 v0.9.5 或更早升级时，磁盘上可能残留旧实现产生的分片/副本；新版本会在下次上传时自动回收，也可手动删除 `AllBackupTemp/*.partNNNNN` 与 `AllBackupTemp/miback/`

---

## 历史版本

| 版本 | 要点 |
| --- | --- |
| v0.9.5 | 123 云盘暂停支持 |
| v0.9.0 | Android 9~17 全版本适配（targetSdk 37 + Hook 多候选兼容） |
| v0.8.1 | 版本号递增与文档修正 |
| v0.8.0 | Gradle 发布版改用腾讯镜像源；115 网盘撤销支持 |
| v0.7.5 | 版本号递增 |
| v0.7.1 | 版本号递增 |
| v0.7.0 | 新增联通沃盘通道 |
| v0.6.0 | 新增天翼云盘（189）与百度网盘通道 |
| v0.5.0 | 云盘账号层与凭据加密存储 |
| v0.2.1 / v0.2.0 | 早期版本：SMB / WebDAV / 自定义脚本三通道 |
